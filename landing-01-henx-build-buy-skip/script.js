const manual=document.querySelector('.decision-card'); document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',()=>document.body.classList.add('has-moved'))); if(manual){manual.addEventListener('click',()=>{manual.classList.toggle('is-open'); manual.querySelector('small').textContent=manual.classList.contains('is-open')?'Decision record / expanded':'Decision record / sample';});}
